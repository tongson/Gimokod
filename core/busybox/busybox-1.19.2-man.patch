--- busybox-1.19.2/miscutils/man.c
+++ busybox-1.19.2/miscutils/man.c
@@ -116,7 +116,7 @@
 	/* "2>&1" is added so that nroff errors are shown in pager too.
 	 * Otherwise it may show just empty screen */
 	cmd = xasprintf(
-		man ? "gtbl | nroff -Tlatin1 -mandoc 2>&1 | %s"
+		man ? "mandoc -Tutf8 2>&1 | %s"
 		    : "%s",
 		pager);
 	system(cmd);
