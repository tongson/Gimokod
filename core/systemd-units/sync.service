[Unit]
Description=sync
DefaultDependencies=no
After=systemd-random-seed-save.service
Before=shutdown.target

[Service]
Type=oneshot
ExecStart=/bin/sync
ExecStart=/bin/echo 3 > /proc/sys/vm/drop_caches
ExecStart=/bin/echo 0 > /proc/sys/vm/drop_caches
ExecStart=/bin/mount -o remount,ro /
